<div>
    <h3>User registration</h3>
    @* Helper methods - which generate HTMl on our behalf *@
    @* Old way for generating form*@
    @{
        Html.BeginForm("CreateUser", "User", FormMethod.Post);}
        <div>
            @Html.Label("fullname","Full Name")
            @Html.TextBox("fullname")
        </div>
        <div>
            @Html.Label("age", "Age in years")
            @Html.TextBox("age", null, new { @type = "number" })
        </div>
        <div>
            @Html.Label("address", "Address")
            @Html.TextArea("address",null, new {rows="5", cols="30"})
        </div>
        <div>
            @Html.Label("email", "Primary Email")
        @Html.TextBox("email", null, new { @type = "email" })
        </div>
        <input type="submit" value="Create User" />
    @{Html.EndForm();}
    @*  <form action="/User/CreateUser" method="post">
        <div>
            <label for="fullname"><b>Full Name</b></label>
            <input name="fullname" id="fullname" type="text" />
        </div>
        <div>
            <label for="age"><b>Age in years</b></label>
            <input name="age" id="age" type="number" />
        </div>
        <div>
            <label for="address"><b>Address</b></label>
            <textarea name="address" id="address" rows="5" cols="30"></textarea>
        </div>
        <div>
            <label for="email"><b>Email</b></label>
            <input name="email" id="email" type="email" />
        </div>
        <hr />
        <input type="submit" value="Create User" />
    </form> *@
</div>
